# Required for real LLM calls. Without it, the agent falls back to a
# heuristic router and a templated response built directly from evidence.
ANTHROPIC_API_KEY=

# Optional overrides.
LLM_PROVIDER=anthropic
LLM_MODEL=claude-haiku-4-5-20251001
